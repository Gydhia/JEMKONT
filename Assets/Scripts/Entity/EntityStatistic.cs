using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum EntityStatistics
{
   Health = 0,
   Shield = 1,
   Mana = 10,
   Speed = 11,
   Strength = 20,
   Defense = 30,
   Range = 31
}
